<h1>Julia's Twitter</h1>

<ul>
    <% @tweets.each do |tweet| %>
        <li>
            <strong><%= tweet.handle %></strong> : <%= link_to tweet.content, tweet_path(tweet) %>
        </li>
    <% end %>
</ul>
